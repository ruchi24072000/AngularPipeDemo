import { TempratureConverterPipe } from './temprature-converter.pipe';

describe('TempratureConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TempratureConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
